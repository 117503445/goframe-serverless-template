version: "3.9"
services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile_dev
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./:/root/project/goframe-serverless-template
    stdin_open: true
    tty: true